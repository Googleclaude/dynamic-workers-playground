node_modules/
.wrangler/
.DS_Store
dist/
worker-configuration.d.ts
.claude/settings.local.json

# Secrets — never commit credentials to the repository
.env
.env.*
!.env.example
# Wrangler local secrets (alternative to `wrangler secret put`)
.dev.vars
.dev.vars.*
*.pem
*.key
*.p12
*.pfx
credentials.json
credentials*.json
*secret*
*_secret
